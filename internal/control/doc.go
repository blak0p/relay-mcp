// Package control resolves the finite allowlist of terminal control keys to
// their exact byte sequences. It accepts normalized key names only; it never
// accepts arbitrary byte input.
package control
